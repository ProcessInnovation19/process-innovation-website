---
name: sync-apf-foundation
description: Deprecated alias — sync Foundry foundation into a project. Same as sync-foundation.
---

# sync-apf-foundation (alias)

This skill is a **deprecated alias** of `sync-foundation`.

Use:

```powershell
.\scripts\sync-foundation.ps1 -TargetPath "<project>"
```

See `.cursor/skills/sync-foundation/SKILL.md` in the foundation package.
